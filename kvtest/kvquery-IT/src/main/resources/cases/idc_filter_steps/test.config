#
# Contains tests about filter steps
#

before-ddl-file   = before.ddl
before-data-file  = before.data

var-$bool = false
var-$work = "work"
var-$city = "city"

run-filtering-steps =   q() = q

after-ddl-file   = after.ddl
