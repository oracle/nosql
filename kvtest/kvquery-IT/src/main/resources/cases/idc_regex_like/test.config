#
# Contains tests about regex_like().
#

before-ddl-file   = before.ddl
before-data-file  = before.data

run-ins = q() = expres

#compile-ins-plans = q() = explans

after-ddl-file = after.ddl

var-$inputStr = "Password1"
var-$inputJson = {"name": "Ajay"}
var-$inputBool = true
