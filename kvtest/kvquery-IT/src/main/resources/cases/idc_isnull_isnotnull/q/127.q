#Expression is map filter step on a nested map with .values().values() and $value implicit variable with is not null in projection
select s.children.Anna.values().values($value = "age") is not null from sn s