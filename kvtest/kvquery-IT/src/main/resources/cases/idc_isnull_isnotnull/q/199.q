#Expression has logical and arithmetic operator with is not null in predicate and is null, is not null in projection
select id, s.children.values().Relative is not null,s.map.IDP is null from sn s where age >50 is not null or s.map.IDP[].DL<="P" is not null