#Expression is map filter step on a nested map with .values().values() and $ ,$key as implicit variable with is null in predicate
select id from sn s where  s.children.values().values($.age=10 and $key="age") is null