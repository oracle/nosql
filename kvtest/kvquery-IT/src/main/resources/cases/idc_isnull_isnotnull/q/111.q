#Expression is map filter step with .values() and and $key implicit variable with is null in predicate
select id,s.children.values($key = "Matt") from sn s where  s.children.values($key = "Anna") is null