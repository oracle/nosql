#
# Contains tests about field steps, slice steps and filter steps
#

before-ddl-file   = complexTable-before.ddl
before-data-file  = complexTable-before.data

run-simpleUserTable =   q() = q

after-ddl-file   = complexTable-after.ddl
