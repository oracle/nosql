#
# Contains tests for multi-row updates
#

before-ddl-file   = before.ddl
before-data-file  = before.data

run-inner_join = q() = expres

compile-inner_join = q() = explans

after-ddl-file = after.ddl

