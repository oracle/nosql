#
# Contains tests about slice steps
#

before-ddl-file   = before.ddl
before-data-file  = before.data

var-$bool   = false
var-$phones = "phones"
var-$map    = "map"

run-filtering-steps =   q() = expres

after-ddl-file   = after.ddl
